<h1>Update {{flight.flightNum}}:</h1>
<form id="update-flight" action="/flights" method="post">
    Flight Number: <input type="text" name="flightNum" value="{{flight.flightNum}}"><br>
    Aircraft: <select name="aircraft" id="aircraft-selector"> 
        {{#each aircraft}}
        <option value="{{id}}">{{registrationNumber}}</option>
        {{/each}}
    </select><br>
    Departure City: <input type="text" name="departureCity" value="{{flight.departureCity}}"><br>
    Arrival City: <input type="text" name="arrivalCity" value="{{flight.arrivalCity}}"><br>
    Date and Time: <input type="datetime-local" name="dateTime" value="{{flight.arrivalCity}}"><br>
</form>
<button onclick="updateflight({{flight.id}})">Update</button>
<script defer>selectAircraft({{aircraft}});</script>
